import React from 'react'
import { View, Text } from 'react-native';
import { Button } from 'react-native-paper';

export const AccountScreen = () => {
    return(
        <View>
            <Button>This is Account Screen</Button>
            
        </View>
    );
};